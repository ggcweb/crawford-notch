/**
 * External booking provider (Campspot). Every booking call-to-action across
 * the site points here so the destination only needs to change in one place.
 */
export const BOOKING_URL = 'https://www.campspot.com/book/crawfordnotchcampground';
